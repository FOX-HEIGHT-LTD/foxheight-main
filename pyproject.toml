[tool.poetry]
name = "foxheight"
version = "0.1.0"
description = "FOX HEIGHT AGI platform"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
# Add your dependencies here

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"